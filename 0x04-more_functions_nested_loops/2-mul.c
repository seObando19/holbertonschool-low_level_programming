#include "holberton.h"
/**
  * mul - function for multiply two int
  *@a: value 1 to multiply
  *@b: value 2 to multiply
  *Return: result of multiply
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
